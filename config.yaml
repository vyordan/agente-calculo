# Configuración del sistema

chunk_size: 800
chunk_overlap: 100
top_k_chunks: 5                 # BM25 usará este valor

models_cache_dir: "./models_cache"
use_qwen_math: true

# Parámetros del generador
generation_temperature: 0.3
generation_num_beams: 4
response_mode: "generative"